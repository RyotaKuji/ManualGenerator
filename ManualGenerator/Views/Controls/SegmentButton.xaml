<Grid x:Class="SegmentedButton"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:ManualGenerator"
      mc:Ignorable="d"
      Loaded="OnLoaded">

	<Grid.RowDefinitions>
		<RowDefinition Height="*"/>
		<RowDefinition Height="*"/>
	</Grid.RowDefinitions>

	<Grid.Resources>
		<Style x:Key="SegmentStyle"
		       TargetType="{x:Type Border}">
			<Setter Property="BorderBrush" Value="DodgerBlue"/>
			<Setter Property="Padding" Value="3,6"/>
			<Style.Triggers>
				<!--  選択  -->
				<Trigger Property="IsEnabled" Value="False">
					<Setter Property="Background" Value="DodgerBlue"/>
				</Trigger>
				<!--  非選択  -->
				<MultiDataTrigger>
					<MultiDataTrigger.Conditions>
						<Condition Binding="{Binding IsEnabled, RelativeSource={RelativeSource Self}}" Value="True"/>
						<Condition Binding="{Binding IsMouseOver, RelativeSource={RelativeSource Self}}" Value="False"/>
					</MultiDataTrigger.Conditions>
					<Setter Property="Background" Value="White"/>
					<Setter Property="BorderThickness" Value="1.5"/>
				</MultiDataTrigger>
				<!--  非選択・マウスオーバー  -->
				<MultiDataTrigger>
					<MultiDataTrigger.Conditions>
						<Condition Binding="{Binding IsEnabled, RelativeSource={RelativeSource Self}}" Value="True"/>
						<Condition Binding="{Binding IsMouseOver, RelativeSource={RelativeSource Self}}" Value="True"/>
					</MultiDataTrigger.Conditions>
					<Setter Property="Background" Value="WhiteSmoke"/>
					<Setter Property="BorderThickness" Value="1.5"/>
				</MultiDataTrigger>
			</Style.Triggers>
		</Style>
		<Style x:Key="LabelStyle"
		       TargetType="{x:Type TextBlock}">
			<Style.Triggers>
				<!--  選択  -->
				<DataTrigger Binding="{Binding IsEnabled, RelativeSource={RelativeSource AncestorType=Border}}"
				             Value="False">
					<Setter Property="Foreground" Value="White"/>
				</DataTrigger>
				<!--  非選択  -->
				<DataTrigger Binding="{Binding IsEnabled, RelativeSource={RelativeSource AncestorType=Border}}"
				             Value="True">
					<Setter Property="Foreground" Value="Silver"/>
				</DataTrigger>
			</Style.Triggers>
		</Style>
	</Grid.Resources>

	<Border x:Name="Segment1"
	        Grid.Row="0"
	        MouseDown="Clicked"
	        Style="{StaticResource SegmentStyle}"
	        CornerRadius="5,5,0,0">
		<DockPanel>
			<TextBlock Text="{Binding Item1Text, RelativeSource={RelativeSource AncestorType=Grid}}"
			           FontSize="20"
			           TextAlignment="Center"
			           Style="{StaticResource LabelStyle}"
			           DockPanel.Dock="Top"/>
			<TextBlock Text="{Binding Item1Annotation, RelativeSource={RelativeSource AncestorType=Grid}}"
			           FontSize="10"
			           TextAlignment="Center"
			           Style="{StaticResource LabelStyle}"
			           DockPanel.Dock="Bottom"/>
		</DockPanel>
	</Border>

	<Border x:Name="Segment2"
	        Grid.Row="1"
	        MouseDown="Clicked"
	        Style="{StaticResource SegmentStyle}"
	        CornerRadius="0,0,5,5">
		<DockPanel>
			<TextBlock Text="{Binding Item2Text, RelativeSource={RelativeSource AncestorType=Grid}}"
			           FontSize="20"
			           TextAlignment="Center"
			           Style="{StaticResource LabelStyle}"
			           DockPanel.Dock="Top"/>
			<TextBlock Text="{Binding Item2Annotation, RelativeSource={RelativeSource AncestorType=Grid}}"
			           FontSize="10"
			           TextAlignment="Center"
			           Style="{StaticResource LabelStyle}"
			           DockPanel.Dock="Bottom"/>
		</DockPanel>
	</Border>
</Grid>
