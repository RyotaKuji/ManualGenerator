<Grid x:Class="ProcessGroup"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:ManualGenerator"
      mc:Ignorable="d"
      xmlns:sf="http://schemas.syncfusion.com/wpf"
      Margin="12,10">

	<Grid.RowDefinitions>
		<RowDefinition Height="Auto"/>
		<RowDefinition Height="Auto"/>
	</Grid.RowDefinitions>
	<Grid.ColumnDefinitions>
		<ColumnDefinition Width="Auto"/>
		<ColumnDefinition Width="*"/>
		<ColumnDefinition Width="*"
		                  MaxWidth="300"/>
	</Grid.ColumnDefinitions>

	<local:StyledButton Grid.Row="0"
	                    Grid.Column="0"
	                    Grid.RowSpan="2"
	                    Width="35"
	                    Height="35"
	                    Margin="8"
	                    VerticalAlignment="Center">
		<Label Content="－"/>
	</local:StyledButton>
	<sf:SfTextBoxExt x:Name="Heading"
	                 Text="{Binding Heading}"
	                 Watermark="見出し"
	                 Grid.Row="0"
	                 Grid.Column="1"
	                 LostFocus="Heading_LostFocus"
	                 Height="35"
	                 Margin="8,0"
	                 FontSize="25"
	                 BorderThickness="0"/>
	<sf:SfTextBoxExt Text="{Binding Description}"
	                 Watermark="説明"
	                 TextWrapping="Wrap"
	                 AcceptsReturn="True"
	                 Grid.Row="1"
	                 Grid.Column="1"
	                 MinHeight="80"
	                 Margin="8,0"
	                 BorderThickness="0"/>
	<local:StyledButton Grid.Row="0"
	                    Grid.Column="2"
	                    Grid.RowSpan="2"
	                    FontSize="50">
		<Label Content="＋"
		       FontSize="70"
		       Foreground="DarkGray"
		       Padding="0"/>
	</local:StyledButton>
</Grid>
